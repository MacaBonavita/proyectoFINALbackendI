<div>
    <h1>Hola, {{user.name}} {{user.lastname}}.</h1>
    {{#if isAdmin}}
        {{#each food}}
        <div>
            <p>Prod. Name: {{this.name}}</p>
            <p>Price: ${{this.price}}</p>
        </div>
        {{/each}}
    {{else}}
    <p>No eres "admin" pero Bienvenido!</p>
    {{/if}}
</div>